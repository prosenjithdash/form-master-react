/*
1. Use form tag and onSubmit handler with event (e) and  name to get values

2. controller element : use individual field state for each input field

3. Controller element : one object  in a state holding all the input fields value

4. Uncontrolled element : useRef to create a reference to the element and access value by using like: nameRef.current.value

5. use hook to handle states and submit and error. Example react hook form

6. Reusable component
*/